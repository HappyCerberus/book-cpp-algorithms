# TODO: temporarily disabled, should be valid, but doesn't compile locally
#add_executable(priority_queue priority_queue.cpp)
#install(TARGETS priority_queue DESTINATION bin)

add_executable(span_stringview span_stringview.cpp)
# Compile only
