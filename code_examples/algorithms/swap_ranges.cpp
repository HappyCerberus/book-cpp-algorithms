#include <algorithm>
#include <cassert>
#include <concepts>
#include <vector>
#include <iostream>

int main() {
#include "swap_ranges_code.h"
auto cmp = { 9, 8, 7, 4, 5, 6, 3, 2, 1 };
assert(std::equal(data.begin(), data.end(), cmp.begin()));
std::cerr << ".";
}
