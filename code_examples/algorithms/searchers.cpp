#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <string>
#include <iterator>
#include <list>
#include <span>
#include <functional>

int main() {
#include "searchers_code.h"
assert(it1 == haystack.begin()+1);
assert(it2 == haystack.begin()+1);
assert(it3 == haystack.begin()+1);

std::cerr << ".";
}
