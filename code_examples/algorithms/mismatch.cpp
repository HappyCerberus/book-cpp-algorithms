#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <iterator>

int main() {
#include "mismatch_code.h"
assert(it_pair.first == first.begin()+2);
assert(it_pair.second == second.begin()+2);

std::cerr << ".";
}
