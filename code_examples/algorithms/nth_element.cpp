#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <string>
#include <iterator>

int main() {
#include "nth_element_code.h"	
assert(data[7] == 2);
auto cmp = { 3, 4, 5, 6, 7, 8, 9};
assert(std::is_permutation(data.begin(), data.begin()+7, cmp.begin()));

std::cerr << ".";
}
