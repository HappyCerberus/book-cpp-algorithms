#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <iterator>
#include <ranges>
#include <set>

int main() {
#include "equal_range_code.h"
assert(*lb == 6);
assert(*ub == 7);
assert(std::distance(data.begin(), lb) == 5);
assert(std::distance(data.begin(), ub) == 8);

std::cerr << ".";
}
