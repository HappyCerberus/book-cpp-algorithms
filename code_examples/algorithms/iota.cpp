#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <iterator>
#include <ranges>
#include <numeric>
#include <ranges>
#include <execution>
#include <cmath>

int main() {
#include "iota_code.h"
auto cmp = {-4, -3, -2, -1, 0, 1, 2, 3, 4};
assert(std::ranges::equal(data, cmp));

std::cerr << ".";
}
