#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <string>
#include <iterator>

int main() {
#include "minmax_code.h"
assert(first.rank == 1);
assert(second.rank == 2);
assert(result.first == 5);
assert(result.second == 10);
}
