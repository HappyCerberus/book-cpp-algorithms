#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <iterator>

int main() {
#include "is_heap_code.h"

assert(test1 == false);
assert(test2 == true);
assert(it1 == std::next(data.begin()));
assert(it2 == data.end());

std::cerr << ".";
}
