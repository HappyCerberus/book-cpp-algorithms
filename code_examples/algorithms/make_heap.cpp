#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <iterator>

int main() {
#include "make_heap_code.h"
assert(std::is_heap(data.begin(), data.end(), std::greater<>{}));

std::cerr << ".";
}
