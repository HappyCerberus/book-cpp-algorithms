#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <string>

int main() {
#include "partition_copy_code.h"
auto cmp1 = {2, 4, 6};
auto cmp2 = {1, 3, 5};
assert(std::equal(even.begin(), even.end(), cmp1.begin()));
assert(std::equal(odd.begin(), odd.end(), cmp2.begin()));

std::cerr << ".";
}
