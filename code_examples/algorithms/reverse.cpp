#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <iterator>
#include <ranges>
#include <memory>
#include <numeric>

int main() {
#include "reverse_code.h"
auto cmp = {7, 6, 5, 4, 3, 2, 1};
assert(std::ranges::equal(data, cmp));

std::cerr << ".";
}
