#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <iterator>
#include <ranges>
#include <numeric>
#include <ranges>
#include <execution>
#include <cmath>

int main() {
#include "move_code.h"
std::vector<std::string> cmp{ "", "", "", "a", "b", "c" };
assert(std::ranges::equal(data, cmp));

std::cerr << ".";
}
