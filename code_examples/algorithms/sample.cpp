#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <iterator>
#include <ranges>
#include <numeric>
#include <ranges>
#include <execution>
#include <cmath>
#include <list>
#include <random>

int main() {
#include "sample_code.h"
assert(out.size() == 5);
assert(std::is_sorted(out.begin(), out.end()));

std::cerr << ".";
}
