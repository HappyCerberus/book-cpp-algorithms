#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <iterator>
#include <ranges>
#include <numeric>
#include <ranges>
#include <execution>

int main() {
#include "set_symmetric_difference_equal_code.h"
assert(equal_symmetric_difference.size() == 2);
assert(equal_symmetric_difference[0].label == "second_b");
assert(equal_symmetric_difference[1].label == "first_c");

std::cerr << ".";
}
