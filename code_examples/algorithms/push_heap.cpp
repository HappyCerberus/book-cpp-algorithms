#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <iterator>

int main() {
#include "push_heap_code.h"
assert(std::is_heap(data.begin(), data.end()-2));
assert(data[4] == 7);
assert(data[5] == 9);

std::cerr << ".";
}
