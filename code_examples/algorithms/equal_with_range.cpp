#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <iterator>

int main() {
#include "equal_with_range_code.h"
assert(test1);
assert(!test2);
assert(pair_it.first == first.end());
assert(pair_it.second == second.begin()+5);

std::cerr << ".";
}
