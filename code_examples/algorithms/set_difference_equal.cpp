#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <iterator>
#include <ranges>
#include <numeric>
#include <ranges>
#include <execution>

int main() {
#include "set_difference_equal_code.h"
assert(equal_difference.size() == 2);
assert(equal_difference[0].label == "first_c");
assert(equal_difference[1].label == "first_d");

std::cerr << ".";
}
