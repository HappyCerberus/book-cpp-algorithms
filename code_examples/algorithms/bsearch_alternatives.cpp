#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <iterator>
#include <ranges>
#include <set>

int main() {
#include "bsearch_alternatives_code.h"
assert(exist);
assert(candidate == &data[3]);
assert(lb == &data[3]);
assert(ub == &data[4]);

std::cerr << ".";
}
