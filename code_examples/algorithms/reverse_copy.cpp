#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <iterator>
#include <ranges>
#include <numeric>
#include <ranges>
#include <execution>
#include <cmath>
#include <list>
#include <random>

int main() {
#include "reverse_copy_code.h"
auto cmp = { 9, 8, 7, 6, 5, 4, 3, 2, 1 };
assert(std::ranges::equal(out, cmp));

std::cerr << ".";
}
