#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <string>
#include <iterator>
#include <list>

int main() {
#include "minmax_extra_code.h"
assert(min == -2);
assert(minmax.first == -2);
assert(minmax.second == 5);
assert(max == 5);

std::cerr << ".";
}
