#include <algorithm>
#include <cassert>
#include <vector>
#include <iostream>
#include <string>
#include <iterator>
#include <list>
#include <span>

int main() {
#include "search_code.h"
	
assert(it1 == haystack.begin()+1);
assert(it2 == haystack.begin()+4);

std::cerr << ".";
}
