#include <algorithm>
#include <cassert>
#include <concepts>
#include <vector>
#include <iostream>

int main() {
#include "partial_sort_code.h"

auto cmp = {9, 8, 7, 6, 5, 4, 3, 2, 1};
assert(std::is_permutation(data.begin(), data.end(), cmp.begin()));
assert(std::equal(data.begin(), data.begin()+3, cmp.begin()));
std::cerr << ".";
}
