#include <vector>
#include <iterator>
#include <concepts>
#include <ranges>
#include <list>
#include <cassert>
#include <algorithm>
#include <random>

int main() {
#include "rangified_code.h"
auto cmp = {1, 2, 3, 4};
assert(std::ranges::equal(cmp, dt));
}
