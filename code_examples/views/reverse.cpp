#include <iostream>
#include <algorithm>
#include <ranges>
#include <unordered_map>
#include <vector>
#include <cassert>

int main() {
#include "reverse_code.h"
    auto cmp1 = {4, 3, 2, 1};
    assert(std::ranges::equal(out, cmp1));
}
